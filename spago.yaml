package:
  name: bubbletea
  dependencies:
    - effect
    - prelude
    - maybe
    - arrays
    - functions
  build:
    strict: true
    pedantic_packages: true

workspace:
  package_set:
    registry: 49.0.0
  backend:
    cmd: echo "This package uses purescript-native Go backend"
    dependencies: []